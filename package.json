{
  "name": "circl-docs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint:oas": "spectral lint 'openapi/**/*.yaml'",
    "schema:validate": "ajv validate --spec=draft2020 --all-errors --strict=false --validate-formats=false -s schemas/universal/product.schema.json -d examples/products/get-by-id.json",
    "docs:build": "mkdocs build --strict"
  },
  "devDependencies": {
    "@stoplight/spectral-cli": "^6.15.0",
    "ajv": "^8.17.1",
    "ajv-cli": "^5.0.0",
    "ajv-formats": "^3.0.1",
    "glob": "^11.0.0",
    "markdownlint-cli": "^0.41.0"
  }
}
