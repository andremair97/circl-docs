{
  "name": "circl-docs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint:oas": "spectral lint 'openapi/**/*.yaml'",
    "validate:examples": "node .github/validate-examples.mjs",
    "map:off": "node scripts/off-map.mjs",
    "generate:examples": "npm run map:off && node .github/validate-examples.mjs",
    "docs:build": "mkdocs build --strict"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "glob": "^11.0.0",
    "@stoplight/spectral-cli": "^6.15.0",
    "markdownlint-cli": "^0.41.0"
  }
}
